import 'package:flutter/material.dart';

class AppPadding {
  static const zero = EdgeInsets.all(0.0);
  static const all24 = EdgeInsets.all(24.0);
  static const all16 = EdgeInsets.all(16.0);
  static const horizontal24 = EdgeInsets.symmetric(
    horizontal: 24.0,
  );
  static const verticall24 = EdgeInsets.symmetric(
    vertical: 24.0,
  );
  static const right8 = EdgeInsets.only(
    right: 8.0,
  );
}
